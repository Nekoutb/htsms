<!doctype html><html lang="{{ app()->getLocale() }}"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><meta name="theme-color" content="#0c1f18"><link rel="icon" href="{{ asset('favicon.svg') }}" type="image/svg+xml"><link rel="alternate icon" href="{{ asset('favicon.ico') }}"><title>@yield('title') · EA HTSMS</title>@vite(['resources/css/app.css','resources/js/app.js'])</head>
<body class="auth-page">@php($fr = app()->isLocale('fr'))
<a class="brand auth-brand" href="{{ route('home') }}"><img src="{{ asset('brand/ea-mark.svg') }}" alt=""><span>EA HTSMS</span></a>
<div style="position:absolute;right:28px;top:28px;z-index:5" class="lang-switch"><a class="{{ !$fr ? 'active' : '' }}" href="{{ route('locale.switch','en') }}">EN</a><a class="{{ $fr ? 'active' : '' }}" href="{{ route('locale.switch','fr') }}">FR</a></div>
<main class="auth-shell"><section class="auth-story"><span class="eyebrow">{{ $fr ? 'Votre SIM. Votre passerelle.' : 'Your SIM. Your gateway.' }}</span><h1>{{ $fr ? 'Communiquez avec vos clients grâce à une infrastructure que vous contrôlez.' : 'Message customers from infrastructure you control.' }}</h1><p>{{ $fr ? 'Associez un téléphone Android, créez une clé API sécurisée et suivez chaque message.' : 'Pair an Android phone, issue a secure API key, and follow every message through delivery.' }}</p><div class="auth-proof"><span>✓</span><div><b>{{ $fr ? 'Isolation par entreprise' : 'Tenant-isolated by default' }}</b><small>{{ $fr ? 'Chaque appareil, clé et message appartient à un seul espace.' : 'Every device, key, and message belongs to one workspace.' }}</small></div></div></section><section class="auth-card">@if(request()->boolean('verified'))<div class="flash success">{{ $fr ? 'Adresse e-mail vérifiée. Vous pouvez vous connecter.' : 'Email address verified successfully. You can now sign in.' }}</div>@elseif(session('status'))<div class="flash success">{{ session('status') }}</div>@endif @yield('content')</section></main>
</body></html>
