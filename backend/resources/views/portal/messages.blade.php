@extends('layouts.portal')
@section('title','Messages') @section('heading','Messages')
@section('actions')<a class="button small" href="#compose">Compose message</a>@endsection
@section('content')
<section id="compose" class="panel compose"><div class="panel-head"><div><span>Transactional SMS</span><h2>Compose a message</h2></div><small>Use E.164 numbers such as +237670000000</small></div><form method="POST" action="{{ route('portal.messages.send',$organization) }}">@csrf<div class="compose-grid"><label>Recipient<input name="to" value="{{ old('to') }}" placeholder="+237 6•• ••• •••" required></label><label>Schedule (optional)<input type="datetime-local" name="send_at" value="{{ old('send_at') }}"></label>@if(count($simSlots) > 1)<label>Send from<select name="sim_slot"><option value="">Any available SIM</option>@foreach($simSlots as $index => $label)<option value="{{ $index }}" @selected(old('sim_slot') === (string) $index)>SIM {{ $index + 1 }} · {{ $label }}</option>@endforeach</select></label>@endif</div><label>Message<textarea name="content" maxlength="1600" rows="4" placeholder="Write a clear transactional message…" required>{{ old('content') }}</textarea><small>Messages longer than one SMS may use multiple segments.</small></label><div class="form-actions"><button class="button">Queue message</button></div></form></section>
<section class="panel"><div class="panel-head"><div><span>Delivery log</span><h2>All messages</h2></div><small>{{ $messages->total() }} records</small></div>@include('portal.partials.message-table',['messages'=>$messages])<div class="pagination">{{ $messages->links() }}</div></section>
<section class="panel"><div class="panel-head"><div><span>Inbound</span><h2>Recent replies</h2></div><small>Latest {{ count($inbound) }}</small></div><div class="table-wrap"><table><thead><tr><th>From</th><th>Message</th><th>Received</th></tr></thead><tbody>@forelse($inbound as $reply)<tr><td class="mono">{{ $reply->sender }}</td><td class="message-body">{{ $reply->body }}</td><td>{{ $reply->received_at?->diffForHumans() }}</td></tr>@empty<tr><td colspan="3" class="empty">No inbound messages yet. Replies from your customers appear here.</td></tr>@endforelse</tbody></table></div></section>
@endsection
