<div class="table-wrap"><table><thead><tr><th>Recipient</th><th>Message</th><th>Status</th><th>Created</th></tr></thead><tbody>@forelse($messages as $message)<tr><td class="mono"><span data-private-number data-value="{{ $message->recipient }}">{{ mb_substr($message->recipient,0,4) }}••••{{ mb_substr($message->recipient,-3) }}</span> <button type="button" class="reveal-link" data-reveal-number>Reveal</button></td><td class="message-body">{{ $message->body }}</td><td><span class="status {{ $message->status->value }}">{{ str_replace('_',' ',$message->status->value) }}</span></td><td>{{ $message->created_at?->diffForHumans() }}</td></tr>@empty<tr><td colspan="4" class="empty">No messages yet. Send your first test message.</td></tr>@endforelse</tbody></table></div>
