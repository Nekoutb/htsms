<!doctype html><html lang="{{ app()->getLocale() }}"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><meta name="theme-color" content="#0c1f18"><link rel="icon" href="{{ asset('favicon.svg') }}" type="image/svg+xml"><link rel="alternate icon" href="{{ asset('favicon.ico') }}"><title>Create workspace · EA HTSMS</title>@vite(['resources/css/app.css','resources/js/app.js'])</head><body class="auth-page"><a class="brand auth-brand" href="/"><img src="{{ asset('brand/ea-mark.svg') }}" alt=""><span>EA HTSMS</span></a><main class="single-card"><div class="form-heading"><span>Last step</span><h2>Name your workspace</h2><p>Usually your business name. Everything else is optional — you can fine-tune later.</p></div>@if(session('status'))<div class="flash success">{{ session('status') }}</div>@endif<form method="POST" action="{{ route('portal.organizations.store') }}" class="stack-form">@csrf<label>Business name<input name="name" value="{{ old('name') }}" placeholder="Acme Cameroon" required autofocus></label><details class="advanced-options" {{ $errors->any() ? 'open' : '' }}><summary>Optional settings</summary><label>Workspace ID<input name="slug" value="{{ old('slug') }}" placeholder="Leave blank to generate automatically"><small>Short identifier used in links, e.g. acme-cameroon.</small></label><div class="form-grid"><label>Timezone<select name="timezone">@foreach(['Africa/Douala','Africa/Lagos','Africa/Abidjan','Africa/Accra','Africa/Kinshasa','Africa/Nairobi','Africa/Johannesburg','Europe/Paris','UTC'] as $zone)<option value="{{ $zone }}" @selected(old('timezone','Africa/Douala') === $zone)>{{ $zone }}</option>@endforeach</select></label><label>Language<select name="locale"><option value="en" @selected(old('locale','en') === 'en')>English</option><option value="fr" @selected(old('locale') === 'fr')>Français</option></select></label></div></details>@if($errors->any())<div class="form-error">{{ $errors->first() }}</div>@endif<button class="button full">Open my workspace</button></form><form method="POST" action="{{ route('logout') }}" class="signout-foot">@csrf<span>Signed in as {{ auth()->user()?->email }}</span><button type="submit" class="danger-link">Sign out</button></form></main></body></html>
