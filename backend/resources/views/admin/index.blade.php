<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><meta name="csrf-token" content="{{ csrf_token() }}"><title>Platform administration · HTSMS</title>@vite(['resources/css/app.css','resources/js/app.js'])</head><body class="admin-page"><header class="admin-header"><a class="brand" href="{{ route('admin.index') }}"><span>HT</span>SMS</a><div><b>Platform operations</b><a href="{{ route('portal.home') }}">Customer portal</a><form method="POST" action="{{ route('logout') }}">@csrf<button>Sign out</button></form></div></header><main class="admin-main">@if(session('status'))<div class="flash success">{{ session('status') }}</div>@endif<h1>Platform administration</h1><p class="lead">Subscription review, account safety, and operational controls.</p><section class="panel"><div class="panel-head"><div><span>Action required</span><h2>Pending plan requests</h2></div><b>{{ $pendingRequests->count() }}</b></div><div class="admin-requests">@forelse($pendingRequests as $change)<article><div><strong>{{ $change->organization?->name }}</strong><span>requests {{ ucfirst($change->requested_plan) }} · {{ $change->created_at?->diffForHumans() }}</span></div><form method="POST" action="{{ route('admin.requests.approve',$change) }}">@csrf<button class="button small">Confirm payment & activate</button></form><form method="POST" action="{{ route('admin.requests.reject',$change) }}">@csrf<button class="danger-link">Reject</button></form></article>@empty<div class="empty">No subscription requests pending.</div>@endforelse</div></section><section class="panel"><div class="panel-head"><div><span>Tenants</span><h2>Business workspaces</h2></div><small>{{ $organizations->total() }} total</small></div><div class="table-wrap"><table><thead><tr><th>Workspace</th><th>Plan</th><th>Usage</th><th>Devices</th><th>Controls</th></tr></thead><tbody>@foreach($organizations as $org)<tr><td><strong>{{ $org->name }}</strong><small class="block">{{ $org->slug }}</small></td><td><span class="status {{ $org->subscription?->status === 'active' ? 'delivered' : 'queued' }}">{{ $org->subscription?->plan ?? 'none' }} / {{ $org->subscription?->status ?? 'unconfigured' }}</span></td><td>{{ number_format($org->messages_count) }} messages</td><td>{{ $org->devices_count }}</td><td><div class="admin-controls"><form method="POST" action="{{ route('admin.organizations.pause',$org) }}">@csrf<button>{{ $org->sending_paused_at ? 'Resume' : 'Pause sending' }}</button></form><form method="POST" action="{{ route('admin.organizations.suspend',$org) }}">@csrf<button class="danger-link">{{ $org->suspended_at ? 'Unsuspend' : 'Suspend' }}</button></form></div></td></tr>@endforeach</tbody></table></div><div class="pagination">{{ $organizations->links() }}</div></section></main></body></html>
